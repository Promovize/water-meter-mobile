import { StyleSheet, Text, View } from "react-native";
import React from "react";

const LodingContainer = () => {
  return (
    <View style={{}}>
      <Text>LodingContainer</Text>
    </View>
  );
};

export default LodingContainer;

const styles = StyleSheet.create({});
